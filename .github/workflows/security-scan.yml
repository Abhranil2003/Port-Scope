name: 🔐 Security Scan

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  bandit-scan:
    name: 🛡️ Bandit Security Scan
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: 🐍 Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 📦 Install dependencies
        run: |
          pip install -r requirements.txt

      - name: 🔒 Install Bandit
        run: |
          pip install bandit

      - name: 🧪 Run Bandit scan and output SARIF
        run: |
          bandit -r . -f sarif -o bandit-results.sarif

      - name: 📤 Upload Bandit SARIF results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v2
        with:
          sarif_file: bandit-results.sarif
